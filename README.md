# tg-running-tracker

Telegram-бот для учёта беговых тренировок и просмотра статистики.  

## Возможности
- Регистрация пользователя
- Добавление тренировок (дистанция)
- Просмотр статистики
- Интерактивное управление через Telegram-кнопки

## Технологии
- Go
- Telegram Bot API
- PostgreSQL (хранение данных)

## Запуск проекта

### Требования
- Go 1.20+
- PostgreSQL 13+
- Telegram Bot Token

### Настройка окружения
Проект использует переменные окружения:

- `BOT_TOKEN` — токен Telegram-бота
- `DATABASE_URL` — строка подключения к PostgreSQL

Пример:

```bash
export BOT_TOKEN="your_telegram_bot_token"
export DATABASE_URL="postgres://USER:PASSWORD@localhost:5432/DBNAME?sslmode=disable"
```

### Подготовка базы данных
1) Создай базу данных в PostgreSQL (любым удобным способом).  
2) Примени SQL-скрипт с таблицами:

```bash
psql "$DATABASE_URL" -f sql/001_Create.sql
```

### Запуск
```bash
go run cmd/tg/main.go
```

## Структура проекта
```text
cmd/tg/            // точка входа
internal/
  handlers/        // обработчики сообщений и callback-кнопок
  service/         // бизнес-логика
  storage/         // контракт + реализации хранилища
  telegram/        // работа с Telegram API
sql/               // SQL-скрипты (создание таблиц)
```

## Статус проекта
🚧 В разработке

Планируется:
- Покрытие тестами
- Docker / docker-compose
- Расширение функционала